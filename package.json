{
  "name": "ning-ai-product-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node scripts/validate-content.mjs && npm run build",
    "test:e2e": "playwright test",
    "capture:cases": "node scripts/capture-case-assets.mjs",
    "generate:resume": "node scripts/generate-resume-pdf.mjs"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@vitejs/plugin-react": "^6.1.0",
    "vite": "^8.2.2"
  }
}
